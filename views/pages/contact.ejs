<!DOCTYPE html>
<html lang="en">
	<head>
		<%- include('../partials/_head', { title: title || 'Contact' }) %>
	</head>
	<body>
		<header>
			<%- include('../partials/_nav') %>
		</header>

		<main class="container contact-page">
			<h1>Contact</h1>

			<% if (typeof message !== 'undefined' && message) { %>
				<div class="message <%= message.type %>"><%= message.text %></div>
			<% } %>

			<form method="POST" action="/contact" class="contact-form">
				<label for="name">Name</label>
				<input id="name" name="name" required value="<%= (form && form.name) ? form.name : '' %>" />

				<label for="email">Email</label>
				<input id="email" name="email" type="email" required value="<%= (form && form.email) ? form.email : '' %>" />

				<label for="message">Message</label>
				<textarea id="message" name="message" rows="5" required><%= (form && form.message) ? form.message : '' %></textarea>

				<button type="submit">Send</button>
			</form>
		</main>

		<footer>
			<%- include('../partials/_footer') %>
		</footer>
	</body>
</html>
